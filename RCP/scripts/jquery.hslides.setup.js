// Override default settings
$((function () {
    $('#accordion').hSlides({
        totalWidth: 900,
        totalHeight: 300,
        minPanelWidth: 111,
        maxPanelWidth: 456,
		easing: "easeOutBack",
		activeClass: 'current'
    });
}));